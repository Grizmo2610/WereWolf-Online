// Extracted from the redesigned static mockups (index/register/lobby/room.html)
export const theme = {
  colors: {
    bgFrom: '#1b2735',      // body background gradient start
    bgTo: '#090a0f',        // body background gradient end
    cardBg: 'rgba(17, 20, 30, 0.75)',   // glass card background
    cardBorder: 'rgba(255, 255, 255, 0.08)',
    text: '#e2e8f0',        // body text
    textStrong: '#f1f5f9',  // headings, names
    textMuted: '#94a3b8',   // labels
    textFaint: '#64748b',   // footer text
    textFaintest: '#475569', // placeholders, hints
    accentRed: '#e43f3f',
    accentOrange: '#ff7b00',
    inputBg: 'rgba(255, 255, 255, 0.05)',
    inputBorder: 'rgba(255, 255, 255, 0.1)',
    success: '#22c55e',     // ready status
    warning: '#f59e0b',     // waiting status
    danger: '#f87171',      // leave room / error
  },
  font: "'Segoe UI', Tahoma, Geneva, Verdana, sans-serif",
}
